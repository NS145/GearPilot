PORT=5000
MONGODB_URI=mongodb://localhost:27017/gear-pilot
JWT_SECRET=gearpilot_secret_key_2026

# Instructions for New Laptops:
# 1. Copy this file to a new file named .env
# 2. Ensure MongoDB is installed and running on the machine
# 3. Run 'npm install' then 'npm run seed' in the server directory
